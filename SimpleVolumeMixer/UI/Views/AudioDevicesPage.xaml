<UserControl x:Class="SimpleVolumeMixer.UI.Views.AudioDevicesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimpleVolumeMixer.UI.Views"
             xmlns:viewModels="clr-namespace:SimpleVolumeMixer.UI.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:AudioDevicesViewModel}"
             Style="{StaticResource BaseUserControlStyle}"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <UserControl.Resources>
        <ItemsPanelTemplate x:Key="VerticalAudioDevicesPanelTemplate">
            <StackPanel Orientation="Horizontal" />
        </ItemsPanelTemplate>
        <DataTemplate x:Key="VerticalAudioDeviceDataTemplate">

        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <GroupBox Header="Devices">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Hidden">
                <ItemsControl ItemsSource="{Binding Devices}"
                              ItemsPanel="{StaticResource VerticalAudioDevicesPanelTemplate}"
                              ItemTemplate="{StaticResource VerticalAudioDeviceDataTemplate}"/>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</UserControl>
